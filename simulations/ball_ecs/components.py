class Ball:
    def __init__(self, x, y, dx, dy, radius, gravity):
        self.x = x
        self.y = y
        self.dx = dx
        self.dy = dy
        self.radius = radius
        self.gravity = gravity
